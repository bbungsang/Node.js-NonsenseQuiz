<!DOCTYPE html>
<html lang="ko">
<% include ./base.ejs %>
<body>
    <h1>Loop it!</h1>
    <ul>
        <% for (var i=0; i<length; i++) { %>
            <li>
                <%= "LOOP" + i %>
            </li>
        <% } %>
    </ul>
</body>
</html>
